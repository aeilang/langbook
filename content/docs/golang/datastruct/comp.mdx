---
title: 复合类型
---

光有砖和瓦还不够，我们还需要墙、门、板这些模型，才能快速构建起我们的大楼。我们需要把基本类型组合起来，这就是复合类型。

## 1. 结构体  [#struct]

使用`struct`来定义一个结构体类型。结构体是多个相关属性的集合，比如，人有年龄, 体重这些属性。

```go
type person struct {
	age    int32
	height int32
}
```

<Callout title="type 定义类型">

你可以使用`type`关键件创建一个新的类型，例如给`int32`类型起一个别名。

```go
type myint int32
```

`myint `类型的底层是`int32`, 但这两个不是一个类型。

</Callout>

### 1.1 结构体占多少内存? [#much]

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

type person struct {
	age    int32 // 4 byte // [!code highlight]
	height int32 // 4 byte // [!code highlight]
}

func main() {
	p := person{
		age:    18,
		height: 160,
	}
	fmt.Println(unsafe.Sizeof(p))
}

```

运行，输出(64位系统):

```
8
```

`person`结构体占用8个字节。我们知道`age` 和`height`是`int32类型`，都占4个字节， 加起来正好8个字节。

![](@/public/images/struct1.svg)

<Callout title="结构体怎么初始化">

1. 当我们只声明结构体时，go自动赋予结构体零值。结构体的零值就是包含的字段的零值

```go title="main.go"
package main

import (
	"fmt"
)

type person struct {
	age    int32
	height int32
}

func main() {
	var p person // [!code highlight]
	fmt.Printf("%#v\n", p) // %#v 表示打印p变量的详细信息
}
```

运行，输出

```
main.person{age:0, height:0}
```

2. 使用结构体字面量初始化

```go
p := person{
		age:    18,
		height: 160,
}
```

`person{age: 18, height: 160}`就是结构体的字面量，以类型开头，大括号里写入各个字段的值。

如果有的字段没有指明，就会赋予这个字段对应类型的零值：

```go
p := person{
    age: 18
} // p: person{age: 18, height: 0}
```

3. 读取和修改结果体的字段

```go title="main.go"
package main
import (
	"fmt"
)
type person struct {
	age    int32
	height int32
}
func main() {
	p := person{age: 18, height: 160} 

	// 读取
	age := p.age // age = 18 // [!code highlight]
	fmt.Println(age)

	// 修改
	p.age = 35 // [!code highlight]
	fmt.Println(p) // {age: 35, height: 160}
}
```

运行，输出

```
18
{35 160}
```

</Callout>

我们可以设想，不包含字段的结构体即 空结构体不占内存，验证一下

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

type empty struct { // 空结构体 
}

func main() {
	p := empty{}
	fmt.Println(unsafe.Sizeof(p))
}
```

运行，输出

```
0
```

果然，**空结构体不占内存！**， 这在并发编程传递取消信号时很有用，因为不占内存，就减少了复制开销。我们在传递取消信号时，不关心传递了什么类型的变量，只关心有东西传递过来了。

### 1.2 结构体内存对齐规则  [#memo]

**规则1**：

如果把person结构体的`age`的类型改为`int8`, 结构体会占用多少内存呢? 根据上面的经验，应该占1+4=5 byte

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

type person struct {
	age    int8  // 1 byte // [!code highlight]
	height int32 // 4 byte
}

func main() {
	p := person{age: 18, height: 160}
	fmt.Println(unsafe.Sizeof(p))
}
```

运行，输出

```
8
```

然而更改后的结构体占8个字节，而不是5个字节。因为这里涉及到内存对齐，规则1: **整个结构体的大小必须是最大字段大小的整数倍。 这里的最大字段必须是基本类型(如整数，布尔， 浮点数等，不包括`string`)**

`person结构体`占内存最大的是`height`, 4 个字节，整体必须是4的倍数，又要加1个字节，所以是8个字节。 

![](@/public/images/uint.svg)

**规则2：**

我们看两个有趣的现象，看看结构体的字段不同的排列方式如何影响占用内存:

![](@/public/images/two.png)

这里引出规则2: **每个字段的起始位置必须是其类型大小的整数倍**。结构体内存对齐受到两个规则的约束，这启示我们，把占内存大的字段排在前面，（其实咋排都没关系，影响不大，我们知道原理就好。）

## 2. 数组 [#array]

为什么会有数值类型？当我们需要一组顺序排序的元素，需要快速地读取和修改第几个元素时 (例如书架上的书)， 就需要用到数组类型。

###  2.1 数组的定义

数组的类型是`[len]T`, len为数组的长度，T为包含元素的类型:

```go title="main.go"
package main

import "fmt"

func main() {
    // 创建一个长度为5，类型为int的数组 // [!code highlight]
	arr := [5]int{1, 2, 3, 4, 5} // [!code highlight]
	fmt.Println(arr)
}
```

输出

```
[1 2 3 4 5]
```

也可以省略长度，用`...`代替，go会帮我们自动推断出长度

```go
arr := [5]int{1, 2, 3, 4, 5}
```

等价于

```go
arr := [...]int{1, 2, 3, 4, 5}
```

声明空数组会默认初始化为它的零值

```go title="main.go"
package main

import "fmt"

func main() {
	var arr [5]int // [!code highlight]
	fmt.Println(arr)
}
```

输出

```
[0 0 0 0 0]
```

### 2.2 数组的读取和修改

```go title="main.go"
package main

import "fmt"

func main() {
	arr := [5]int{1, 2, 3, 4, 5} 
    a1 := arr[0] // a1 = 1, 读取第一个元素 // [!code highlight]
	fmt.Println(a1)
    
    arr[0] = 9 // 修改arr的第一个元素为9 // [!code highlight]
    fmt.Println(arr)
}
```

输出

```
1
[9 2 3 4 5]
```

### 2.3 数组的索引越界

```go title="main.go"
package main

import "fmt"

func main() {
	arr := [5]int{1, 2, 3, 4, 5}
	a1 := arr[5] // 5越界，最大的索引为4     // [!code highlight]
	fmt.Println(a1)
}
```

运行, 编译期 panic:

```
index 5 out of bounds
```

![](@/public/images/arr.svg)

### 2.4 数组的长度是其类型的一部分

```go title="main.go"
package main

import "fmt"

func main() {
	var arr [5]int
	arr2 := [2]int{1, 2} 
	arr = arr2 // 不同长度的数组是不同的类型 // [!code highlight]
	fmt.Println(arr)
}
```

运行，编译期panic:

```
cannot use arr2 (variable of type [2]int) as [5]int value in assignment
```

### 2.5 数组占多少内存

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

func main() {
	arr := [5]int{1, 2, 3, 4, 5}
	fmt.Println(unsafe.Sizeof(arr))
}

```

运行，输出

```
40
```

每个int类型占8个字节，一共有5个，5*8=40 个字节。数组在内存中是连续分布的，方便根据索引来快速访问和修改元素。

![](@/public/images/arr2.svg)

所以数组占用内存 = 元素占用的内存 * 数组长度。

## 3. 切片 [#slice]

有意思的部分来了。因为数组的长度就是它类型的一部分，我们不能增加和删除元素，所以Go创造了切片。我们使用最多的就是切片，而不是数组。

### 3.1 切片的定义

切片的类型是`[]T`， T可以是任何类型。和数组不同，切片的长度不是类型的一部分。

- 声明一个切片但没初始化，go会赋予它的零值，`nil`。`nil`就是没有的意思。

```go title="main.go"
package main

import "fmt"

func main() {
	var s []int // [!code highlight]
	fmt.Println(s == nil)
}
```

运行，输出

```
true
```

- 使用切片的字面量声明并初始化：

```go title="main.go"
package main

import "fmt"

func main() {
	s := []int{1, 2, 3} // [!code highlight]
	fmt.Println(s)
    fmt.Println(len(s)) // len(s) 返回切片的长度 3
	fmt.Println(cap(s)) // cap(s) 返回切片的容量 3
}
```

输出

```·
[1 2 3]
3
3
```

`[]int{1, 2, 3}`就是切片的字面量，它定义了切片的类型和包含的元素。

- 使用`make([]T, len, cap)`声明并初始化

```go title="main.go"
package main

import "fmt"

func main() {
	// 创建一个长度为2，容量为4的[]int类型的切片
	s := make([]int, 2, 4)
	fmt.Println(s) // [0, 0]
	fmt.Println(len(s)) // len(s) 返回切片的长度 2
	fmt.Println(cap(s)) // cap(s) 返回切片的容量 4
}
```

输出

```
[0 0]
2
4
```

`make`会初始化对应长度的零值。

- 也可以省略容量，`make([]T, len)`这时容量等于长度。

```go title="main.go"
package main

import "fmt"

func main() {
	// 创建一个长度为2，容量为2的[]int类型的切片
	s := make([]int, 2) // [!code highlight]
	fmt.Println(s) // [0, 0]
	fmt.Println(len(s)) // len(s) 返回切片的长度 2
	fmt.Println(cap(s)) // cap(s) 返回切片的容量 2
}
```

输出

```
[0 0]
2
2
```

切片的读取、修改和索引越界与数组相同，这里不再赘述。（切片的底层就是数组）

### 3.2 增加和删除元素

- 使用`append`函数增加

```go title="main.go"
package main

import (
	"fmt"
)

func main() {
	// 定义一个长度为0的空切片
	s := make([]int, 0)
	fmt.Printf("开始s：, len=%d, cap=%d\n", len(s), cap(s)) // %d打印整数。

    // 依次增加10个元素，i从0到9
	for i := range 10 {
		s = append(s, i)
		fmt.Printf("增加第%d个元素： len=%d, cap=%d\n", i+1, len(s), cap(s))
	}
}
```

输出

```
开始s：, len=0, cap=0
增加第1个元素： len=1, cap=1
增加第2个元素： len=2, cap=2
增加第3个元素： len=3, cap=4
增加第4个元素： len=4, cap=4
增加第5个元素： len=5, cap=8
增加第6个元素： len=6, cap=8
增加第7个元素： len=7, cap=8
增加第8个元素： len=8, cap=8
增加第9个元素： len=9, cap=16
增加第10个元素： len=10, cap=16
```



<Callout title="切片扩容策略">

对应扩容前的切片(`ls`), 设

- 它的长度`len0 = len(ls)`

- 容量`cap0 = cap(ls)`
- `append()`函数要添加的元素个数为`n`

| 切片的容量 | 扩容后的长度               |
| ---------- | -------------------------- |
| 小于256    | `max(len0+n, 2 * cap0) `   |
| 中间地带   | `2*cap0 -> 1.25*cap0` 过渡 |
| 大容量数组 | 约等于 `1.25*cap0`         |

简单来说，小容量切片扩容容量翻倍，大容量切片扩容容量翻1.25倍。中间地带是平滑过渡的曲线。有`runtime`包为证:

```go title="runtime/slice.go"
// nextslicecap 计算下一个适当的切片长度。
func nextslicecap(newLen, oldCap int) int {
    newcap := oldCap
    doublecap := newcap + newcap
    if newLen > doublecap {
        return newLen
    }

    const threshold = 256
    if oldCap < threshold {
        return doublecap
    }
    for {
        // 从小切片的2倍增长过渡到大切片的1.25倍增长。
        // 这个公式在两者之间提供了一个平滑的过渡。
        newcap += (newcap + 3*threshold) >> 2

        // 我们需要检查 `newcap >= newLen` 以及 `newcap` 是否溢出。
        // newLen 保证大于零，因此当 newcap 溢出时，
        // `uint(newcap) > uint(newLen)` 。
        // 这使得我们可以通过相同的比较来检查这两者。
        if uint(newcap) >= uint(newLen) {
            break
        }
    }

    // 当 newcap 计算溢出时，设置 newcap 为请求的 cap 值。
    if newcap <= 0 {
        return newLen
    }
    return newcap
}
```

为什么要设置这样的扩容策略？切片的底层是数组，每次扩容都要新建一个数组，把旧的数组复制到新的数组，所以要尽量扩容大一些。但当容量很大时，扩容两倍的数组又很浪费空间，所以扩容1.25倍。

</Callout>

你可以一次性增加多个元素:

```go title="main.go"
package main

import "fmt"

func main() {
	ls := []int{1, 2, 3}
	ls = append(ls, 4, 5, 6) // [!code highlight]
	fmt.Println(ls)
}
```

输出

```
[1 2 3 4 5 6]
```

记得把`append`返回的切片赋值给原切片`ls`

也可以把一个切片的元素添加到另一个切片，使用`...`操作符解开

```go title="main.go"
package main

import "fmt"

func main() {
	ls1 := []int{1, 2, 3}
	ls2 := []int{4, 5, 6}

	ls1 = append(ls1, ls2...) // ...把切片解开 // [!code highlight]
	fmt.Println(ls1)
}
```

输出

```
[1 2 3 4 5 6]
```

### 3.3 切片占多少内存

使用`unsafe.Sizeof()`查看切片占多少字节的内存

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

func main() {
	ls1 := []int{1, 2, 3}
	ls2 := []int{}

	fmt.Println(unsafe.Sizeof(ls1)) 
	fmt.Println(unsafe.Sizeof(ls2)) 
}
```

输出

```
24
24
```

其实，**所有的切片都占`24`个字节**。为什么呢? 我们看看切片是怎么实现的

```go title="runtime/slice.go"
type slice struct {
	array unsafe.Pointer // 指针: 8 byte， 指向底层数组 
	len   int            // 8byte
	cap   int            // 8byte
}
```

根据结构体内存对齐的规则，切片就占`8+8+8=24`个字节。

已下面的切片`ls`为例

```go
ls := []int8{1,2,3}
```

它在内存中是这样的:

![](@/public/images/s1.svg)

### 3.4 切片的常见错误

思考一个问题，下面的切片`ls`会改变吗

```go title="main.go"
package main

import "fmt"

func x2(ls []int) {
	for i, v := range ls { // i 是ls 的索引，v 是ls的值
		ls[i] = v * 2 
	}
}

func main() {
	ls := []int{1, 2, 3}
	x2(ls)
	fmt.Println(ls)
}
```

我们将`ls`传入`x2`函数，这个函数作用是将`ls`的每个元素乘2。

运行， 输出

```
[2 4 6]
```

ls改变了，为什么会改变呢，我们并没有传入`ls`的指针`*[]int`呀。原因是切片的实现就包含有指针指向底层数组。我们知道，Go中一切传输都是进行复制:

![](@/public/images/s2.svg)

聪明的你，下面的`ls`会改变吗?

```go title="main.go"
package main

import "fmt"

func add(ls []int) {
	ls = append(ls, 4)
}

func main() {
	ls := []int{1, 2, 3}
	add(ls) 
	fmt.Println(ls)
}
```

输出

```
[1 2 3]
```

为什么ls 不会改变呢。首先`append`函数会先判断`ls`底层还有没有足够的容量来存`4`。显然是没有的，因为`ls`的长度和容量都是`3`。那么`append`就会新建一个底层数组，如图：

![](@/public/images/s3.svg)

底层数组没有改变，所以main函数里的`ls`不会改变。

为了使外面的`ls`改变，我们让`add`函数返回`ls`即可，像`append`函数一样:

```go title="main.go"
package main

import "fmt"

func add(ls []int) []int { // [!code highlight]
	ls = append(ls, 4)
    return ls // [!code highlight]
}

func main() {
	ls := []int{1, 2, 3}
	ls = add(ls) // [!code highlight]
	fmt.Println(ls)
}
```

输出

```
[1 2 3 4]
```

将接片作为参数传入函数要特别小心，函数有没有`append`操作，如果有，切片是否有足够的容量来存放新的切片。如果没有，那么`append`会进行扩容，返回一个新的切片，外面的切片就不会改变。



## 4. 字符串 [#string]

字符串虽然是基本类型，但它的实现和切片差不多，我们来看看吧。

### 4.1 字符串占多少内存呢？

```go title="main.go"
package main

import (
	"fmt"
	"unsafe"
)

func main() {
	var str1 string // 空字符串 ""
	str2 := "hello world"

	fmt.Println(unsafe.Sizeof(str1))
	fmt.Println(unsafe.Sizeof(str2))
}
```

运行，输出

```
16
16
```

我们发现，所以的`string`都占16个字节，为什么呢？我们来看它的实现:

```go
type string struct {
	array unsafe.Pointer // 指针 8byte
	len   int            // 8 byte
}
```

string是一个结构体，根据结构体内存对齐的规则，它占`8+8=16`个字节。

已下面的`str`为例:

```go
str := "hello"
```

它在内存中是这样的:

![](@/public/images/str1.svg)

和切片不同的是,`string`是没有容量的。为什么呢，Go在设计时就让`string`不能变:

```go title="main.go"
package main

import (
	"fmt"
)

func main() {
	str := "hello world"
	str[0] = 'o' // 编译期报错
	fmt.Println(str)
}
```

输出，编译期错误panic:

```
cannot assign to str[0] (neither addressable nor a map index expression
```

### 4.2 字符串相加？

聪明的你，两个字符串相加会发生什么?

```go main.go
package main

import (
	"fmt"
)

func main() {
	str1 := "hello"
	str2 := "world"
	str := str1 + str2
	fmt.Println(str)
}
```

字符串是不能变的，相加会创建新的底层数组:

![](@/public/images/str2.svg)

## 5. map [#map]
